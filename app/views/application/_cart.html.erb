<div class="cart-body">
    <div class="cart-title">
        <h4>Panier</h4>
    </div>
    <div class="cart-elements">
        <% @cart.each do |item| %>
            <p><%= item %></p>
        <% end %>
    </div>
</div>